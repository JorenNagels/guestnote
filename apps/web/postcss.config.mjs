/**
 * The whole file. Tailwind v4 is CSS-first: there is no `tailwind.config.js`, and the
 * theme lives in `@theme` inside design-system/tokens.css.
 */
export default { plugins: { '@tailwindcss/postcss': {} } }
